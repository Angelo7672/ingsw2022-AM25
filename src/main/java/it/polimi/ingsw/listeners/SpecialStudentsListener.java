package it.polimi.ingsw.listeners;

public interface SpecialStudentsListener {
    void specialStudentsNotify(int special, int color, boolean addOrRemove);    //addOrRemove true add on the card, false remove from the card
}
